POSTGRES_DB=articles_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
DATABASE_URL=postgresql+psycopg2://postgres:postgres@db:5432/articles_db
MINDSDB_URL=http://mindsdb:47334

# Optional CSV schema mapping overrides (set only if your dataset uses different headers)
# CSV_COL_TITLE=headline
# CSV_COL_SOURCE=publisher
# CSV_COL_CATEGORY=topic
# CSV_COL_LANGUAGE=lang
# CSV_COL_PUBLISHED_AT=publish_date
# CSV_COL_READING_TIME_MINUTES=read_time
# CSV_COL_URL=link
# CSV_COL_CONTENT=body
